import DiagnosticsPanel from "../DiagnosticsPanel";
import PageShell from "../PageShell";

export default function DiagnosticsPage() {
  return (
    <PageShell title="機器テスト">
      <DiagnosticsPanel />
    </PageShell>
  );
}
